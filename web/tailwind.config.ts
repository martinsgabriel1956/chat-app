import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{jsx,tsx}"],
  theme: {
    extend: {
      colors: {},
    },
  },
  plugins: [],
};
export default config;
